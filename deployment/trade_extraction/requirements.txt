# Amazon Bedrock AgentCore SDK
# Pinned to verified working versions (see deployment/swarm_agentcore/DEPENDENCY_VERSIONS.md)
bedrock-agentcore==1.1.1
bedrock-agentcore-starter-toolkit==0.2.4

# Strands Agents Framework
# Note: use_aws tool not available in current versions, using fallback implementation
strands-agents==1.19.0
strands-agents-tools==0.2.17

# AWS SDK
boto3>=1.40.0

# LLM providers
anthropic>=0.69.0

# Data validation
pydantic>=2.11.0

# Utilities
python-dotenv>=1.1.0
structlog>=25.4.0
