# Amazon Bedrock AgentCore SDK
bedrock-agentcore>=1.1.1
bedrock-agentcore-starter-toolkit>=0.2.4

# Strands Agents Framework with OpenTelemetry support for auto-instrumentation
strands-agents[otel]>=1.19.0
strands-agents-tools>=0.1.0

# AWS Distro for OpenTelemetry (ADOT) - Required for AgentCore Observability
aws-opentelemetry-distro>=0.2.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0

# AWS SDK
boto3>=1.40.0

# LLM providers
anthropic>=0.69.0

# Data validation
pydantic>=2.11.0

# Utilities
python-dotenv>=1.1.0
structlog>=25.4.0

# Testing framework
hypothesis>=6.0.0
pytest>=9.0.2
requests>=2.31.0  # Required for test_dev.py HTTP testing

# Local development only (not needed in AgentCore runtime)
# fastapi>=0.118.0
# uvicorn>=0.37.0
