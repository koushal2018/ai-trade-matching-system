# Trade Matching Swarm - AgentCore HTTP Orchestrator Dependencies
# This orchestrator calls deployed AgentCore agents via HTTP - no Strands SDK needed

# Bedrock AgentCore - Runtime wrapper and observability
bedrock-agentcore>=1.0.0

# HTTP client for agent-to-agent calls
httpx>=0.24.0

# AWS SDK - For SigV4 signing and credentials
boto3>=1.34.0

# Data validation and serialization
pydantic>=2.0.0
